# cc-session-browser configuration — copy to .env and adjust.

# Comma-separated list of Origin headers the API will accept (CORS).
# Default is loopback only. Add LAN/Tailscale/etc. to enable remote access.
ALLOWED_ORIGINS=http://127.0.0.1:8766,http://localhost:8766
